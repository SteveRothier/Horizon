# OpenWeather API key (optional) — fallback when Open-Meteo fails
# Create a free key: https://openweathermap.org/api
# Used only on the server (Route Handlers). Never expose as NEXT_PUBLIC_*.
OPENWEATHER_API_KEY=
